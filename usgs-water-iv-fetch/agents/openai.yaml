interface:
  display_name: "USGS Water IV Fetch"
  short_description: "Fetch USGS Water Services instantaneous values"
  default_prompt: "Use $usgs-water-iv-fetch to fetch USGS Water Services instantaneous hydrology observations for a bbox or site list, then return structured JSON."
